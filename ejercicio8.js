function aleatorio( min, max) {
    return Math.round(Math.random(Math.floor) * (max - min) + min);
  }

  document.write(aleatorio(1,50));

  document.write("<br>");

  function aleatorio( min, max) {
    return Math.random(Math.floor) * (max - min) + min;
  }

  document.write(aleatorio(1,50));
  document.write("<br>");

  function aleatorio( min, max) {
    return Math.random(Math.floor) * (max - min) + min;
  }

  document.write(aleatorio(1,50));
  document.write("<br>");

  function aleatorio( min, max) {
    return Math.random(Math.floor) * (max - min) + min;
  }

  document.write(aleatorio(1,50));
  document.write("<br>");

  function aleatorio( min, max) {
    return Math.random(Math.floor) * (max - min) + min;
  }

  document.write(aleatorio(1,50));

